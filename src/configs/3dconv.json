{
    "inference_from_checkpoint_only": 0,
    "model_name": "lit_3dconv",
    "output_dir": "../trained_models/",
    "checkpoint_path": "-1",

    "data_folder": "../data/classification_dot_5classes_train_10000seqs_20_per_seq/",
    "test_data_folder": "../data/classification_1dot_5classes_train_1000seqs_20_per_seq/",

    "nb_train_samples": 9000,
    "nb_val_samples": 1000,

    "num_workers": 128,

    "batch_size": 64,
    "clip_size": 20,
    "nb_labels": 5,

    "hidden_per_layer": [1, 1, 1],
    "kernel_size_per_layer": [3, 3, 3],
    "conv_stride": 1,
    "pooling": ["max", "avg", "max"],

    "optimizer": "Adam",
    "lr": 0.0001,
    "reduce_lr": 0,
    "last_lr": 0.00001,
    "momentum": 0.9,
    "num_epochs": 100,

    "early_stopping_patience": 5,
    "weight_decay": 0.00001,

    "dropout_encoder": 0,
    "dropout_classifier": 0,

    "input_spatial_size": 64
}
