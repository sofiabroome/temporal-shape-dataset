{
    "model_name": "lit_3dconv",
    "output_dir": "../trained_models/",
    "checkpoint_path": "-1",

    "data_folder": "../data/classification_train_10seqs_30_per_seq/",

    "nb_train_samples": 9,
    "nb_val_samples": 1,

    "num_workers": 0,

    "batch_size": 5,
    "clip_size": 20,
    "nb_labels": 3,

    "optimizer": "Adam",
    "lr": 0.01,
    "reduce_lr": 0,
    "last_lr": 0.00001,
    "momentum": 0.9,
    "num_epochs": 1000,

    "early_stopping_patience": 1000,
    "weight_decay": 0.00001,
    "dropout": 0,

    "input_spatial_size": 64
}
